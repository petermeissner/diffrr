#' A simple function that only exists for testing.
#'
#' @param x A number.
#' @param y A number.
#' @return The sum of \code{x} and \code{y}.
#' @examples
#' ptest()
ptest <- function(x,y){
  return(Sys.time())
}
